@using X.PagedList
@using X.PagedList.Mvc.Core
@model IPagedList<BMSBT.Models.AdditionalCharge>
@{
    Layout = "~/Views/Shared/_LayoutMaintBills.cshtml";
    ViewData["Title"] = "Additional Charges";
}

<div class="card">
    <div class="card-header d-flex justify-content-between align-items-center">
        <h5 class="mb-0">Additional Charges</h5>
        <a asp-action="Create" class="btn btn-sm btn-success">Create New</a>
    </div>
    <div class="card-body">
        @if (TempData["SuccessMessage"] != null)
        {
            <div class="alert alert-success">
                @TempData["SuccessMessage"]
            </div>
        }

        <form method="get" asp-action="Index" class="form-inline mb-3">
            <div class="form-row align-items-end w-100">
                <div class="col-md-4 col-lg-3">
                    <label for="btNoSearch" class="form-label mb-1">BT No</label>
                    <input type="text" id="btNoSearch" name="btNo" class="form-control" placeholder="Enter BTNo to search" value="@ViewBag.SelectedBtNo" />
                </div>
                <div class="col-auto mt-4 mt-md-0">
                    <button type="submit" class="btn btn-primary">Search</button>
                    <a asp-action="Index" class="btn btn-outline-secondary">Clear</a>
                </div>
            </div>
        </form>

        @if (Model == null || !Model.Any())
        {
            <p>No additional charges found.</p>
        }
        else
        {
            <table class="table table-bordered table-striped table-sm">
                <thead class="table-light">
                    <tr>
                        <th>BT No</th>
                        <th>Service Type</th>
                        <th>Charges Name</th>
                        <th class="text-end">Charges Amount</th>
                        <th style="width:220px;"></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.BTNo</td>
                            <td>@item.ServiceType</td>
                            <td>@item.ChargesName</td>
                            <td class="text-end">@item.ChargesAmount</td>
                            <td>
                                <a asp-action="Edit" asp-route-id="@item.Uid" class="btn btn-sm btn-primary">Edit</a>
                                <a asp-action="Details" asp-route-id="@item.Uid" class="btn btn-sm btn-info">Details</a>
                                <a asp-action="Delete" asp-route-id="@item.Uid" class="btn btn-sm btn-danger">Delete</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>

            <div class="d-flex justify-content-center">
                @Html.PagedListPager(Model, page => Url.Action("Index", new { page, btNo = ViewBag.SelectedBtNo }), new PagedListRenderOptions
                {
                    LiElementClasses = new string[] { "page-item" },
                    PageClasses = new string[] { "page-link" },
                    DisplayLinkToFirstPage = PagedListDisplayMode.Always,
                    DisplayLinkToLastPage = PagedListDisplayMode.Always,
                    DisplayLinkToPreviousPage = PagedListDisplayMode.Always,
                    DisplayLinkToNextPage = PagedListDisplayMode.Always,
                    DisplayEllipsesWhenNotShowingAllPageNumbers = true
                })
            </div>
        }
    </div>
</div>

